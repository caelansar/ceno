import { Req, Res, rust_print } from './types';

async function hello(req: Req): Promise<Res> {
  return {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(req),
  }
}

export { hello };
